# TODO: missing handling for exports.txt breaks at least windows
if host_machine.system() == 'windows'
  subdir_done()
endif

subdir('include')
subdir('pgtypeslib')
subdir('ecpglib')
subdir('compatlib')
subdir('preproc')
